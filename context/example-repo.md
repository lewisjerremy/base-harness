# <repo> Conventions

<!-- TEMPLATE: Per-repo conventions layer on top of the shared parent rules
     (context/CLAUDE.md). Symlink this file into the repo as its local
     CLAUDE.md so agents pick it up:

         ln -s ../base-harness/context/<repo>.md <repo>/CLAUDE.md

     Put here only what differs from the shared rules: stack, build/test/lint
     commands, gotchas, contribution specifics. -->

## Stack

<!-- e.g. framework, language, package manager -->

## Common commands

```sh
# build
# test
# lint / format
```

## Gotchas

<!-- Anything an agent would trip over: env setup, known flaky tests,
     where the ClientApp lives, etc. -->
